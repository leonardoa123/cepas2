<div id="<%= dom_id vino %>">
  <p>
    <strong>Nombre:</strong>
    <%= vino.nombre %>
  </p>

<% vino.ensamblajes.each do |e| %>
  <div>
    <%= e.cepa.nombre + ':' %>
    <%= e.porcentaje %> %
  </div>
<% end %>  

</div>
